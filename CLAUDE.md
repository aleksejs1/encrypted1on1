# CLAUDE.md

Guidance for Claude Code (and other AI assistants) working in this repository.

## Project

encrypted1on1 — a self-hosted, end-to-end encrypted platform for 1:1 meetings between managers and employees. See [README.md](README.md) for the overview, and [`docs/`](docs/) for reader-facing documentation (encryption methodology, user flow, architecture, deployment) — this file stays development notes only; keep new user-facing explanations in `docs/`, not here.

## Current stage

All phases (1–8) and every item in the `private/todo.md` gap-audit backlog (P0–P6) are done. Work since then continued ad hoc (post-`private/todo.md`): password reset/account access (fresh keypair, counterpart re-sharing, in-app change-password), CSP+SRI, dependency scanning, account deletion + data export, demo mode, per-user date-display format, a heading-font fix (Cyrillic/Latvian rendering), and a multi-tenant cloud-service track (`Company` entity, tenant-scoped queries, self-service company creation, platform-admin role, billing scaffolding, cloud deployment topology) that was merged into `main` and released as v1.1.0, with further refinements since (company-level invite settings, a reverse-proxy cloud deployment variant, a load-testing-audit reliability pass) released as v1.1.1, anketa form versioning (`Anketa::CURRENT_FORM_VERSION`, stamped at creation so the question set — starting with the "feelings" field going from 6 to 12 options — can change without altering already-created anketas; see `docs/decisions/2026-08-25-anketa-form-versioning.md`), released as v1.2.0, and opt-in backend error tracking via Sentry (`SENTRY_DSN`, empty/disabled by default — only the Cloud deployment enables it, disclosed as a Privacy Policy sub-processor), released together with company admin reporting Phases 1/2/4 below as v1.2.1, and editable-after-publish anketa answers (a user can now edit their own side's answers at any time before the anketa is archived — publishing itself stays one-way, but is no longer a permanent lock; silent by design, no counterpart notification or "edited" signal of any kind, per an explicit maintainer decision — see `docs/decisions/2026-09-07-editable-published-anketa-answers.md`), in-place editing of individual achievements/growth/discuss list entries (previously add/remove-only — fixing a typo meant delete-and-readd, which silently re-stamped the entry's date and moved it to the end of the log; see [GitHub issue #26](https://github.com/aleksejs1/encrypted1on1/issues/26)), and a fix for the anketa overdue flag flipping on the meeting day itself instead of only the day after (`meetingDate` is stored at UTC midnight but was compared against `now` as a raw instant in all three places that compute it; see [GitHub issue #28](https://github.com/aleksejs1/encrypted1on1/issues/28)) — all released together with admin visibility into sent invites (below), the 7-phase `private/delivery-quality-improvement-proposal.md` internal-tooling work, and the multi-tab-unlock state machine fix (`docs/decisions/2026-08-28-multi-tab-unlock-state-machine.md`) as v1.2.2. `private/todo.md` currently has no open items.

Admin visibility into sent invites (recipient, sender, sent/expiry dates, pending/accepted/expired status, for both company admins and platform admins), released as v1.2.2 — see `docs/decisions/2026-09-07-invite-visibility.md` for the design, including the four open questions from [GitHub issue #24](https://github.com/aleksejs1/encrypted1on1/issues/24) it resolves (self-registration is in scope, CLI/cloud bootstrap is not; a new independent 90-day retention window for the new `InviteRecord` table, decoupled from `ActivationToken`'s own security-driven 24h cleanup; platform admins can see who invited whom across every company; account deletion scrubs a user's own accepted/expired invite history, company-scoped, but never a still-pending duplicate invite).

A small ad-hoc addition after v1.2.2, released as v1.2.3: the anketa list now shows a "days until meeting" label next to each upcoming anketa's date, in both list views — closes [GitHub issue #32](https://github.com/aleksejs1/encrypted1on1/issues/32). Purely client-side (no backend/schema change); a design proposal was drafted and independently technically reviewed twice before the issue was even filed. See `docs/history.md` for the full account, including the 4-round `code-review` loop during implementation — round 1 found a `new Date()`-race between the overdue badge and the day-count label, round 2's fix accidentally froze the displayed day count at page-load time, round 3 fixed both by computing them together, fresh, on every render.

A second small ad-hoc addition: German (`de`) added as a 5th UI language, closing [GitHub issue #34](https://github.com/aleksejs1/encrypted1on1/issues/34) — `frontend/src/i18n/locales/de.json` and `backend/translations/messages.de.yaml` alongside the existing 4, wired into both `SUPPORTED_LOCALES` constants the same way. Demo mode's per-locale sample content was deliberately left out of scope (it needs a genuinely translated narrative regenerated against the live stack, a separate task from translating UI strings) — `frontend/src/demo.ts`'s `demoEmailFor()` now falls back to the English demo account for any UI locale without one. See `docs/history.md` for the full account, including the 3-round `code-review` loop — round 1 caught a lost compile-time exhaustiveness guarantee and a stale comment, round 2 caught a prototype-pollution-shaped lookup bug and missing test coverage, round 3 came back clean.

A third small ad-hoc addition: French (`fr`) added as a 6th UI language, closing [GitHub issue #46](https://github.com/aleksejs1/encrypted1on1/issues/46) — `frontend/src/i18n/locales/fr.json` and `backend/translations/messages.fr.yaml` alongside the existing 5, wired into both `SUPPORTED_LOCALES` constants and `demo.ts`'s `DEMO_EMPLOYEE_EMAILS` map (`fr: null`, same scope limit as German — demo mode falls back to English) the same way German's addition already established the pattern for. See `docs/history.md` for the full account.

A fourth small ad-hoc addition, closing the scope gap the previous two entries each explicitly deferred: German and French demo-mode narrative content — `frontend/scripts/demo-fixture-content.mjs` gained `CONTENT.de`/`CONTENT.fr` (locale-appropriate employee/manager names, the same 2-cycle struggle→resolution narrative every other locale carries, translated with the same formal register — `Sie`/`vous` — the UI translations already established), `demo.ts`'s `DEMO_EMPLOYEE_EMAILS` map no longer carries `de: null`/`fr: null`, and `backend/fixtures/demo-seed.json` was regenerated for all 6 locales against the real dev stack with real crypto. Also updates the sibling `e1o1-landing` repo (a separate git repository, not part of this one) to offer German and French: `src/lib/content/{de,fr}.ts` (full marketing-copy translations, same disclosure convention as the existing `ru`/`lv`/`es` — written by Claude, not yet native-speaker-reviewed) and new `anketa-preview-{de,fr}-{light,dark}.png` hero screenshots, generated by the same `frontend/scripts/generate-doc-screenshots.mjs` this repo already used for the other locales' landing previews. Two unrelated pre-existing script/app-drift issues were found and fixed along the way, not scoped to this feature: `generate-demo-fixture.mjs` still filled meeting/goal dates via a selector and format matching the old native `<input type=date>`, which the per-user date-display-format work above had already replaced with a locale-formatted text field (`design/DateInput.svelte`) that only commits on blur, fixed with a new shared `frontend/scripts/fillDateInput.mjs` (both generation scripts now import it, rather than one growing its own copy of the other's inline fix) — and the same script's post-activation redirect wait used the 30s Playwright default, which running all 6 locales' real argon2id key derivation back-to-back occasionally exceeded, alongside giving each locale its own fresh browser process rather than just a fresh context. Also discovered along the way, not caused by this change: this session's own dev-stack `backend` container crashed (`SIGSEGV`) mid-generation-run more than once, the actual explanation for what first looked like locale-specific Playwright flakiness — an environment-stability issue, not something fixed here. See `docs/history.md` for the full account, including the 7-round `code-review` loop — round 1 caught two stale-documentation gaps and a CLAUDE.md exception-list omission, round 2 caught the German/French dialogue actually using informal `du`/`tu` address rather than the formal register this paragraph claims, plus three independently hand-kept locale lists, round 3 caught the date-fill helper duplication mentioned above, round 4 caught that round 2's own locale-list fix in `ResetDemoDataCommandTest.php` only checked the fixture's internal self-consistency rather than actually cross-checking it against `User::SUPPORTED_LOCALES`, round 5 caught this paragraph's own round-count going stale one round behind reality plus a couple of smaller hardening/dedup gaps, round 6 caught a third argon2id-redirect call site in `generate-doc-screenshots.mjs` that missed round 5's own timeout bump, and round 7 caught the timeout value itself still duplicated as a bare literal across both scripts (now one shared `frontend/scripts/playwrightTimeouts.mjs` constant) plus a locale-suffix test helper reconstructing expected emails by convention instead of reading them from the fixture.

A fifth ad-hoc addition, bundled with everything above since v1.2.2 into v1.2.3: a `DateInput` width fix (`.date-input .input`'s `11ch` didn't account for `.input`'s own padding, clipping the year on every date field app-wide — widened to `calc(10ch + 42px)`), published anketa free-text answers now display as plain text instead of a `<textarea>` (closing [GitHub issue #49](https://github.com/aleksejs1/encrypted1on1/issues/49)), comment threads now default open when they already have comments (closing [GitHub issue #51](https://github.com/aleksejs1/encrypted1on1/issues/51), with a related pre-existing `CommentThread` component-reuse state gap it surfaced filed and deliberately deferred rather than fixed — see `docs/decisions/2026-09-10-comment-thread-reuse-state-deferred.md`), and Markdown formatting for anketa free-text answers (closing [GitHub issue #55](https://github.com/aleksejs1/encrypted1on1/issues/55)) — `marked`+`DOMPurify` with an explicit tag/attribute allowlist (an `img`-only denylist was tried and rejected once review showed a `style` attribute on inline HTML reaches the same unsolicited-network-fetch outcome via CSS), a hand-written toolbar/source-preview editor rather than a third-party editor library (per `docs/adr/0007`), and the accepted plain-text-to-Markdown backward-compatibility trade-offs recorded in `docs/decisions/2026-09-10-anketa-answer-markdown-formatting.md`. See `docs/history.md` for the full account of all five, including a `frontend/package-lock.json` npm-version-drift CI failure the Markdown-formatting work's own lockfile update triggered and fixed.

A sixth ad-hoc addition, released as v1.2.4: live updates on the anketa detail page during a meeting — comments, answer edits, and outcome/checkpoint changes now appear on an already-open tab within one poll interval (4s), closing [GitHub issue #59](https://github.com/aleksejs1/encrypted1on1/issues/59). Short-interval polling of a new `GET /api/anketas/{id}/live-state` endpoint (scalars and the five existing blob version counters only, no schema change), not SSE/Mercure/WebSocket — see `docs/decisions/2026-09-12-anketa-live-updates.md` for the full rejection reasoning, tied to [ADR 5](docs/adr/0005-no-message-queue.md)/[ADR 6](docs/adr/0006-single-frankenphp-container.md). Goals are deliberately excluded (no edit-mode boundary exists for goal editing yet — a real follow-up, not an oversight; a `goalsVersion` column was drafted then dropped after independent review caught the same class of gap). New comments get a brief highlight; everything else stays silent, per an explicit maintainer decision distinguishing this live-meeting scenario from the async editable-published-answers precedent. Also fixed along the way, predating this feature but made meaningfully more likely by it (long-lived, un-reloaded tabs going from an edge case to routine): `saveDraft()`/`publish()` never checked `isArchived()` (a side that never published could keep drafting and even successfully publish onto an already-archived anketa), and `handleArchive()`'s outcome carry-forward read a load-time snapshot that never updated. Built via an unusually long 13-round `code-review` loop — see the decision doc's own "Verification" section for the list of real bugs each round caught, several in the `archived`×`editingMyAnswers` corner of the state machine, the same class of concurrent-state difficulty [the multi-tab-unlock fix](docs/decisions/2026-08-28-multi-tab-unlock-state-machine.md) already documents taking 11 rounds for. See `docs/history.md` for the full account.

A batch of five backend architecture refactors — [GitHub issues #69–73](https://github.com/aleksejs1/encrypted1on1/issues/69): machine-enforced multi-tenant isolation via a Doctrine `SQLFilter` (`App\Doctrine\CompanyFilter`, denormalized `company_id` on `Anketa`), decomposition of the 901-line `AnketaController` into `AnketaRepository`/`GoalRepository`/`AnketaLifecycleService`/`AnketaPresenter`, manual request parsing replaced by typed `#[MapRequestPayload]` DTOs (`src/Dto/`) with `symfony/validator` constraints, `JsonExceptionListener` now formats every unhandled `/api/*` exception as JSON (not just `HttpExceptionInterface` ones), and `AccountDeleter` became an injectable service — were merged directly to `main` (PRs #74–78) without this repo's own mandated pre-merge review loop. Released as v1.2.5, together with a same-week mutation-testing follow-up (PR #91, killing 8 escaped mutants Infection's report-only job had flagged) and everything else accumulated since v1.2.4. A retrospective `code-review` pass afterward found several real regressions the missed review would normally have caught, all fixed in place: CSRF-check-and-rate-limiter ordering broke across ~30 mutating endpoints (`#[MapRequestPayload]`'s validation runs before the controller method body, so the inline `csrfGuard->assertValid()` call each method started with now ran *after* payload validation — fixed by centralizing into a new `CsrfProtectionListener` on `KernelEvents::CONTROLLER`, which runs before argument resolution), six DTOs lost i18n on their validation messages (hardcoded English literals instead of the app's own already-translated `errors.*` keys, now restored, plus one genuinely new key added across all 6 locales), `PlatformAdminController` never re-enabled the tenant filter it deliberately disables for its cross-tenant queries, and a couple of smaller latent gaps (an unreachable-today exception-type mismatch in `AnketaLifecycleService`, a redundant constructor parameter on `Anketa`). A second review round, on that fix commit's own diff, caught that the filter-restore fix was itself buggy (a manual per-return call that thrown `NotFoundHttpException`s skipped entirely) — replaced with a request-attribute read by a new `CompanyFilterListener::onResponse()` so restoration fires exactly once regardless of which exit path a platform-admin action takes — plus two smaller reuse findings (a hand-rolled safe-HTTP-method list replaced with Symfony's own `Request::isMethodSafe()`, and the 6 DTOs' repeated `setTranslationDomain('messages')` calls consolidated into one `App\Dto\DtoViolation::add()` helper). See `docs/decisions/2026-09-15-anketa-controller-decomposition-and-dto-validation.md` for the full list and verification, and `docs/adr/0002-hand-composed-symfony.md`'s updated Consequences section for how the DTO/repository split relates to that ADR's original "boilerplate is an accepted cost" reasoning. See `docs/history.md` for the full account.

Company admin reporting (`private/company-admin-reporting-proposal.md`) is in progress: Phases 1, 2, and 4 (company-wide Overview report, Goals report, `docs/architecture.md` update) are shipped; Phase 3 (Cadence — per-pair 1:1 health) is approved and next; Phase 5 (CSV export) is deferred indefinitely. See the proposal's §12 for the maintainer check-in this was gated on.

`frontend/src/pages/Anketa.svelte`'s 2,522-line monolith was decomposed into `AnketaHeader.svelte`/`AnketaOutcomes.svelte`/`AnketaGoals.svelte`/`AnketaArchiveSection.svelte` plus a few smaller shared pieces (`LockIcon.svelte`, `commentThreadsBusy.ts`, `authorLabel.ts`), closing [GitHub issue #64](https://github.com/aleksejs1/encrypted1on1/issues/64) — a pure decomposition, no behavior/encryption/API change. `Anketa.svelte` itself (~1,200 lines then; its question blocks later moved to `AnswerBlock.svelte`, #149) keeps only what's genuinely cross-cutting: crypto/blob-sync, the live-update poll, and the answer-editing state machine — the same reason `editingOutcomeId`/`confirmingDeleteOutcomeId`/`addingOutcome`/`addingCheckpoint`/`commentThreadsBusy` stay page-level state bound down into the new children rather than moving in fully, since `pollLiveStateFor`'s busy-gates read them directly. See `docs/decisions/2026-09-18-anketa-component-decomposition.md` for the naming deviation from the issue's own suggested `AnketaArchiveModal.svelte` (there's no modal, just two inline cards), the `commentThreadsBusy`-bindable and `isOverdue()`-signature-widening judgment calls, and three pre-existing-but-out-of-scope bugs found along the way (a `handleSaveGoal()` save race with no disabled-while-saving gating or empty-title guard, and two small cross-file CSS/const duplications predating this diff). Verified against the real e2e stack (including a throwaway Playwright spec covering the Outcomes/Goals UI, since nothing in the existing suite touched it) across a 9-round `code-review` loop. Released as v1.2.5. See `docs/history.md` for the full account.

A seventh ad-hoc addition, released as v1.2.5: `App.svelte`'s mount-time `checkAuth()` call — previously a bare `$effect` wrapped in `untrack()` to stop it looping when `checkAuth()`'s synchronous `getGeneration()` read registered as a tracked dependency (a same-tab 401 bumping `generation` via `markSessionExpired()` → `invalidateIdentity()` could otherwise re-trigger the effect) — now uses Svelte's canonical `onMount`, closing [GitHub issue #62](https://github.com/aleksejs1/encrypted1on1/issues/62). A follow-up ([issue #94](https://github.com/aleksejs1/encrypted1on1/issues/94)) found the identical anti-pattern still live at four other `ensureUnlocked()` mount-time call sites (`AdminGate.svelte`, `PlatformAdminPanel.svelte`, `CreateAnketa.svelte`, `AccountSettings.svelte`) and converted those too. See `docs/history.md` for the full account.

An eighth ad-hoc addition, released as v1.2.5: `frontend/src/api/client.ts`'s `apiGet`/`apiPost`/`apiPut`/`apiDelete`/`apiGetAllPages` now accept an optional `signal?: AbortSignal`, plus a shared `abortOnDestroy()`/`isAbortError()` helper (`frontend/src/api/abortOnDestroy.ts`) that aborts a component's mount-time fetch when it's destroyed — closing [GitHub issue #66](https://github.com/aleksejs1/encrypted1on1/issues/66) (first wired into `AnketaList.svelte`/`Report.svelte`/`CreateAnketa.svelte`) and then [issue #95](https://github.com/aleksejs1/encrypted1on1/issues/95) (extended to every other page with the same mount-fetch shape, including `Anketa.svelte`'s own `load()` — its separate live-update poll is untouched, governed by its own interval/staleness guards). Button-triggered writes stay deliberately uncancelled once started, since aborting a mutation doesn't guarantee the server never applied it. See `docs/history.md` for the full account.

A ninth ad-hoc addition, released as v1.2.5: an unrecognized route now renders a `NotFound.svelte` page instead of silently falling through to `AnketaList`/`Login`, closing [GitHub issue #67](https://github.com/aleksejs1/encrypted1on1/issues/67) — `App.svelte`'s inline `PATHS`/route-regex copies were also extracted into a new `frontend/src/routes.ts` (`isKnownPath()`), shared with the `showAppHeader` check that used to duplicate the same logic by hand. `NotFound` is the terminal fallback only: an authenticated-but-locked tab or a logged-out visitor on a mistyped URL still reaches `UnlockTab`/`Login` rather than dead-ending. A same-PR, unrelated CI fix: `actions/setup-node`'s bundled npm on Node 22 still calls the registry's retired `/v1/security/audits/quick` endpoint, now rejected with a 400 — fixed by upgrading npm before `npm audit` runs in the frontend CI job. See `docs/history.md` for the full account.

A tenth ad-hoc addition, released as v1.2.5: `role="alert"` added to every `.banner-error` reachable from the anketa detail page (`Anketa.svelte`, `AnketaOutcomes.svelte`, `CommentThread.svelte`), plus a new `aria-live="polite"` announcer for a comment arriving via the 4s live-update poll (previously a CSS-only highlight with nothing for a screen reader) — closing [GitHub issue #68](https://github.com/aleksejs1/encrypted1on1/issues/68) as rescoped by the maintainer's own comment on it (the issue's separate modal-focus-trapping half was dropped: neither of its two named examples is an actual modal, and no real modal component exists yet to apply `role="dialog"` to). See `docs/history.md` for the full account.

Anketa meeting templates, released as v1.3.0 — [GitHub issues #102–#107](https://github.com/aleksejs1/encrypted1on1/issues/102): a plaintext `Anketa::$templateKey` classifier (SQLite + MySQL migrations) picks one of four built-in question sets (`regular`, `onboarding`, `career_growth`, `support_checkin`) from a picker in `CreateAnketa.svelte`, backed by a `Record<TemplateKey, AnketaTemplate>` registry in `frontend/src/anketa/questions.ts` that must stay in sync with `Anketa::TEMPLATE_KEYS` (a test cross-checks the PHP source). Every template's auto-recreated successor falls back to `regular` (`Anketa::NEXT_CYCLE_TEMPLATE_KEY`, see the two `docs/decisions/2026-09-23-*-template-does-not-recur.md` records). Released alongside it, [GitHub issue #111](https://github.com/aleksejs1/encrypted1on1/issues/111): an anketa created by hand while the pair already has an open one is a persisted one-off (`Anketa::$oneOff`, fixed at creation) — no carry-forward, never auto-recreates — so a pair's chain can't fork; see `docs/decisions/2026-09-23-one-open-anketa-chain-per-pair.md`. Both have dual-actor e2e coverage in `frontend/e2e/dual-actor-anketa.spec.ts`. See `docs/history.md` for the full account.

A post-v1.3.0 fix: unpublished drafts now survive an in-app password change, closing [GitHub issue #129](https://github.com/aleksejs1/encrypted1on1/issues/129). Drafts are encrypted with a key derived from the X25519 private key (`crypto/keypair.ts`'s `deriveDraftKey()`), not with the password-derived master key, so re-wrapping the private key leaves them untouched. Legacy master-key drafts are read through a fallback and re-saved on open, or just before the next password change. An undecryptable draft degrades to an empty draft with a notice, and to `myDraftUnreadable: true` in the export, instead of failing the page. See `docs/decisions/2026-09-24-drafts-survive-password-change.md`.

A post-v1.3.0 fix: an anketa is now archived exactly once, closing [GitHub issue #130](https://github.com/aleksejs1/encrypted1on1/issues/130). A second archive used to return 200 and create a second successor, forking the pair's chain. The controller returns 409, and concurrent requests are settled by `AnketaRepository::markArchivedIfOpen()`, a conditional `UPDATE … WHERE archivedAt IS NULL` in the same transaction as the successor insert. The 409 carries the applied `archivedAt`/`missed`, and the page's live-state poll treats `archived` as one-way. The other mutating anketa endpoints still check `isArchived()` in memory and then write, the same race class; it's deferred along with a few smaller related gaps. See `docs/decisions/2026-09-25-archive-exactly-once.md`.

A post-v1.3.0 change: the read-only anketa view shows only what was answered, closing [GitHub issue #134](https://github.com/aleksejs1/encrypted1on1/issues/134) (part 1 of the [#131](https://github.com/aleksejs1/encrypted1on1/issues/131) design). In the collapsed view (the counterpart's side, my own published side while not editing, both sides once archived) unanswered fields are hidden unless they have comments, an all-empty block shows one "No answer." line, and the generic "Entries"/"Details"/"Anything to add?" captions are dropped above an answer. Rules live in `frontend/src/anketa/answerDisplay.ts`. `AnswerField`'s `collapsed` prop is separate from `readonly` so an in-flight Save never collapses. Each side keeps one keyed `{#each}` per block, not an `{#if}` between two loops. Part 2, [issue #135](https://github.com/aleksejs1/encrypted1on1/issues/135), followed straight after: radio/checkbox answers there show only the chosen options (`selectedOptions()`), and an in-flight Save keeps the disabled controls. See `docs/decisions/2026-09-25-collapsed-read-only-anketa-view.md`.

A post-v1.3.0 fix: comment actions keep keyboard focus, closing [GitHub issue #149](https://github.com/aleksejs1/encrypted1on1/issues/149). Every comment action used to drop focus to `<body>`, not only deleting the last comment on an emptied field. `CommentThread` now moves focus explicitly after each transition: Delete lands on the safe Cancel, a slow request never pulls focus back, and a thread that unmounts hands focus to its question heading through `onFocusLost`. The question block was first extracted from `Anketa.svelte` into `AnswerBlock.svelte`. See `docs/decisions/2026-09-25-comment-thread-keyboard-focus.md`.

A post-v1.3.0 fix: outcome and list-entry actions keep keyboard focus too, closing [GitHub issue #151](https://github.com/aleksejs1/encrypted1on1/issues/151). They use the same approach, now shared as `frontend/src/anketa/keepFocus.ts`. Asynchronous row actions, comments included, check focus against their own row, so a slow save never pulls focus back. A deleted row hands focus to a heading, not a text input, and a held Enter's auto-repeats are ignored in these regions (`ignoreHeldEnter()`). See `docs/decisions/2026-09-25-outcome-and-entry-keyboard-focus.md`.

Private meeting notes ([GitHub issue #132](https://github.com/aleksejs1/encrypted1on1/issues/132)), released as v1.4.0 together with every post-v1.3.0 fix above, shipped in four parts. The first part, [#136](https://github.com/aleksejs1/encrypted1on1/issues/136), is the backend storage and API. A separate `anketa_private_notes` table (`AnketaPrivateNote`) holds a wrapped notes key and a ciphertext blob, one row per anketa per author, and is served only by its own endpoints (`GET`/`PUT /api/anketas/{id}/private-notes`, `GET /api/me/private-notes`). `AnketaPresenter` is unchanged, so no shared payload can carry the counterpart's notes. Part 2, [#137](https://github.com/aleksejs1/encrypted1on1/issues/137), is the panel: a card under the anketa header (`anketa/PrivateNotes.svelte`) with the client crypto (`crypto/privateNotes.ts`), a pure state machine (`anketa/notesState.ts`), its controller (`anketa/notesSession.ts`) and a tab-local encrypted backup (`anketa/notesBackup.ts`); see `docs/decisions/2026-09-26-private-notes-panel.md`. Part 3, [#138](https://github.com/aleksejs1/encrypted1on1/issues/138), makes the panel a sticky right-hand column from 840px (a rail when hidden), and keeps the card below that; see `docs/decisions/2026-09-26-private-notes-layout.md`. Part 4, [#139](https://github.com/aleksejs1/encrypted1on1/issues/139), adds the notes to the data export as a separate `privateNotes` list (`anketa/notesExport.ts`), restores the panel's "Export keeps a copy." link, and documents the feature in `docs/encryption.md` and `docs/user-flow.md`. `docs/decisions/2026-09-26-private-notes.md` is the overview of all four parts. Three points differ from #132's text: validation failures are 400, not 422, overwrites are an atomic conditional `UPDATE`, and the MySQL migration pins its collation by hand. See `docs/decisions/2026-09-26-private-notes-storage-and-api.md`.

Company-authored custom anketa templates ([GitHub issue #133](https://github.com/aleksejs1/encrypted1on1/issues/133), the tracking issue with the full design) are in progress, as five issues in order P1 → C1 → C2 → C3 → C4. P1, [#140](https://github.com/aleksejs1/encrypted1on1/issues/140), is done: the archive form's "Next meeting type" picker. The per-template recurrence map (`Anketa::nextCycleTemplateKeyFor()`) is now only the default, served as the detail's `nextCycleTemplateKey` from `AnketaLifecycleService::defaultNextTemplate()`, and `AnketaController::archive()` resolves the successor's key before any mutation. The client sends `nextTemplateKey` only when changed. Next is C1, [#141](https://github.com/aleksejs1/encrypted1on1/issues/141).

**For the full phase-by-phase and decision-by-decision history — what was built, why, what was tried and rejected, and exactly how each change was verified against a real running stack — see [`docs/history.md`](docs/history.md).** That file carries the detailed record this section used to hold directly; it was split out so a session doesn't have to load the entire development history into context to find the handful of constraints below that are actually still load-bearing. `docs/adr/` holds the ~8 genuinely foundational, hard-to-reverse architecture decisions; `docs/decisions/` holds shorter records of non-trivial decisions made after the split — check both before re-deriving a decision that's already been made.

The detailed spec currently lives outside this repo as a local working document (not tracked in git); an English version is planned to land here (likely `docs/SPEC.md`) as a separate task. Until that exists, treat the constraints below as authoritative, and ask before assuming anything not covered here.

## Non-negotiable constraints

- **End-to-end encryption is the entire point of this product.** The server must never be able to derive plaintext content from what it stores. When in doubt about whether something should be encrypted client-side, assume yes. The one deliberate, narrow exception is a goal's title/description/status/target date (not its progress checkpoints) — nothing else gets that exception without an explicit, discussed product decision.
- **Code must stay simple enough to audit.** This is a privacy tool; its credibility depends on a reasonably technical user being able to read the code and verify the privacy claims themselves. Prefer fewer files, fewer abstractions, and boring solutions over clever ones. Don't add speculative flexibility for requirements that don't exist yet.
- **Repository language is English, without exception** — code, comments, commit messages, docs, issue/PR templates. Working discussions with the maintainer may happen in other languages, but nothing non-English lands in this repo. (The deliberate exceptions: `frontend/src/i18n/locales/{ru,lv,es,de,fr}.json` and `backend/translations/messages.{ru,lv,es,de,fr}.yaml` — translated *user-facing product content*, required by the spec's multilingual requirement, not repository/documentation language; and, the same reasoning, the per-locale `CONTENT` entries in `frontend/scripts/demo-fixture-content.mjs` — translated demo-account narrative text, not documentation, feeding into the same seeded demo data those UI translations describe. Everything surrounding them — keys, code, comments, the rest of that script — stays English.)
- **License is AGPLv3** — see [LICENSE](LICENSE). Don't introduce dependencies or vendored code under incompatible licenses without flagging it first.

## Working style

- This project explicitly favors dumb-and-simple over clever, and has a documented history of scope creep during planning — when a change grows noticeably beyond what was asked, say so before building it, rather than quietly expanding scope.
- Before claiming a change touching anketa keys/sealing/password-derived keys is "verified," see the `e1o1-verify-with-real-crypto` skill — it's this project's own established standard (real crypto against the real stack, not opaque placeholder strings, cleaned up afterward), not a new bar.
- **For a concurrent/async or multi-step-state change (multiple tabs, in-flight requests, session/cache invalidation), design the full state space before writing the fix, not one race at a time as review finds them.** Model the state as a single enum/union over the actual reachable states, not independent booleans that can combine into states nothing produces. Before broadening what a shared invalidation/cleanup function does, grep every existing call site and re-derive whether the new behavior is still correct for each one — not just the call site that prompted the change. The multi-tab unlock fix took 11 `code-review` rounds partly because the first pass modeled unlock state as two booleans (the same "stuck" bug then resurfaced in three different call sites, one round at a time) and broadened a shared cache-clearing function without auditing its other caller (breaking an unrelated admin-panel cache-bust two rounds later) — see `docs/decisions/2026-08-28-multi-tab-unlock-state-machine.md`.
- **Any non-trivial change (new functionality, not a one-line fix) follows this loop before being called done:** implement it, then get it reviewed by an independent agent — not self-review — via the `code-review` skill (it forks a fresh reviewer with no stake in the implementation); fix what it finds; re-review; repeat until a pass comes back with nothing but non-actionable/judgment-call notes. Then run the project's own auto-checks — the same ones CI and `pre-push` run (typecheck, format, lint/knip, unit tests, and where relevant the duplication/doc-link checks) — and confirm they're clean. Write or extend tests for whatever new functionality was added, matching the depth of the existing tests for the pattern being extended (e.g. new mutation functions get the same success/other-items-untouched/wrong-author-throws/wrong-id-throws coverage their sibling functions already have) rather than leaving it to manual verification alone.
- **Enable the git hooks once per clone** (`git config core.hooksPath .githooks`, see `README.md`'s "Git hooks" section) and don't bypass them (`--no-verify`) without a real reason — `pre-commit` autofixes formatting on staged files, `pre-push` runs a scoped typecheck plus a lockfile/manifest-sync check (`npm ci`/`composer validate`) whenever the relevant `package.json`/`composer.json` changed. That second check exists specifically because an incremental `npm install` once left `frontend/package-lock.json` out of sync in a way that passed locally but broke CI's `npm ci` outright (`docs/decisions/2026-08-22-npm-lockfile-drift-pre-push-check.md`) — after editing `package.json`/`composer.json`, verify against a genuinely clean install (`npm ci`, or a fresh `rm -rf node_modules && npm install`), not just an incremental `npm install <pkg>` on top of an already-installed tree.
