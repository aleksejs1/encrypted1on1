/vendor/
/var/
/.env.local
/.env.local.php
/.env.*.local
# Auto-regenerated by symfony/config on every boot — IDE hints only, not source.
/config/reference.php
# PHPUnit's own run-to-run cache (test order/defect tracking) — not source.
/.phpunit.result.cache
/.phpunit.cache/
# PHP-CS-Fixer's own incremental-run cache — not source.
/.php-cs-fixer.cache
